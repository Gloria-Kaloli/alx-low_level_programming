#include "main.h"

/**
 * _islower - A program that checks for lowercase character
 * @c: character to check
 *
 * Return: 1 if c ic lowercase and 0 if not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
