#include <stdio.h>

/**
 * main - program that prints numbers
 * Return: 0 (successful)
 */

int main(void)
{
	int s;

	s = 0;
	while
		 (s < 10) {
			 putchar(s + '0');
			 s++;
			 }
	 putchar('\n');
	 return (0);
}

