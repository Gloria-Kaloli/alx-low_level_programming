#include <stdio.h>

/**
 * main - program that prints alphabet in lower case
 * Return: 0 (successful)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			C++;
		}
	putchar('\n');
	return (0);
}
