#include <stdio.h>

/**
 * main - program that prints alphabet in lower case
 * Return: 0 (successful)
 */

int main(void)
{
	char d;

	d = 'a';
	while
		(d <= 'z') {
			putchar(d);
			c++;
		}
	putchar('\n');
	return (0);
}
