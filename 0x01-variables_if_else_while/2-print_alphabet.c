#include <stdio.h>

/**
 * main - program that prints alphabet in lower case
 * Return: 0 (successful)
 */

int main(void)
{
	char g;

	g = 'a';
	while
		(g <= 'z') {
			putchar(g);
			g++;
		}
	putchar('\n');
	return (0);
}
