#include <stdio.h>
 /**
  * main - program that prints alphabets
  * Retun: 0 (successful)
  */

int main(void)
{
	char t;

	t = 'z';
	while
		(t >= 'a') {
			putchar(t);
			t--;
		}
	putchar('\n');
	return (0);
}
