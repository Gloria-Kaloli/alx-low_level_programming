#include <stdio.h>
/**
 * main - A  program that prints numbers
 * Return: 0 (successful)
 */

int main(void)
{
	char t;

	t = 'z';
	while
		(t >= 'a') {
			putchar(t);
			t--;
		}
	putchar('\n');
	return (0);
}
