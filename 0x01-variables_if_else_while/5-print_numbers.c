#include <stdio.h>
 /**
  * main - program that all single digit number of base10 starting form 0
  * Return: 0 (successful)
  */

int main()
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
