#include "main.h"

/**
 * add - Function that adds teo integers and returns the result
 *
 * @y: number being added
 * @z: number being added
 *
 * Return: Always 0.
 */

int add(int y, int z)
{
	return (y + z);
}

