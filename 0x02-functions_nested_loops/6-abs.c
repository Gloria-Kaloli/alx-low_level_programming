#include "main.h"

/**
 * _abs - Function that computes absolute value of an integer
 *
 * @n: yhe int to check
 *
 * Return: the absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
